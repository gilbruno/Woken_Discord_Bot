import { Alchemy, Network } from 'alchemy-sdk';

export class WokenListener {

    constructor() {
        const settings = {
            apiKey: 'demo', // Replace with your Alchemy API key.
            network: Network.ETH_GOERLI // Replace with your network.
          };
    }

}
